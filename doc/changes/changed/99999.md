- `dune describe workspace` and `dune describe external-lib-deps` now report a
  `public_name`/`public_names` field alongside `name`/`names`. `name`/`names`
  always contain the private name(s) (the `(name ...)` stanza field), while
  `public_name`/`public_names` carry the public name(s) when available.
  (#99999, @nat-karmios)
